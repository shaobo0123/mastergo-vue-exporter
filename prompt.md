你现在要对我提供的导出 Vue 代码做二次整理和响应式改造，输出可维护、可直接接入真实项目的最终 Vue 页面代码。

核心目标：
1. 不要把导出代码当成必须原样保留的最终结果，而是把它整理成真实项目中可维护的页面代码。
2. 如果我额外提供了参考页面、参考组件、参考样式文件、参考截图或现有项目代码，你必须优先对齐这些参考实现的布局风格、组件组织方式、交互层级和样式节奏。
3. 优先级顺序是：现有项目参考实现 / 我明确提供的参考代码 > 设计稿或截图视觉细节 > 导出代码原始结构。

要求如下：
1. 先理解页面在真实项目中的角色，再决定保留、重构或替换哪些导出结构，不要机械照搬导出稿。
2. 如果我提供了参考页面或同类页面代码，最终页面的整体观感、布局骨架、滚动方式、底部操作区位置、区块组织、留白节奏，应尽量向参考实现靠拢。
3. 设计稿、截图或导出代码主要用于补充页面内容、文案、配色、局部视觉和区块关系；如果它们和现有项目风格冲突，优先遵循参考实现或项目规范。
4. 优先复用项目里已有的公共组件和页面骨架，不要重复造轮子。包括但不限于：导航栏、底部操作栏、表单项、卡片、上传组件、信息展示组件、骨架屏、空状态、错误态组件等。
5. 如果参考实现已经有成熟的上传区、信息区、底部按钮区、滚动容器等布局模式，应优先沿用同样的结构思路，而不是重新发明一套页面骨架。
6. 页面结构应接近真实项目页面，而不是设计稿展示页。不要为了“还原效果”手工加入假的业务状态、假的上传结果、假的系统状态栏、假的系统图标或假的预览内容。
7. 不要额外增加参考实现中没有、项目规范中也没有的视觉层，例如：桌面端手机壳、外层展示卡片、悬浮胶囊按钮、大面积阴影、额外装饰背景、夸张圆角容器等。
8. 响应式处理要服务于真实页面落地，重点保证常见移动端宽度下结构稳定、内容可读、模块可伸缩；不要为了响应式而破坏参考页面原本的布局气质。
9. 不要强制套用某一种固定布局写法。根容器、滚动区、底部操作栏、内容区的组织方式，应以参考实现和项目现有习惯为准。
10. 优先使用 flex 或 grid 处理布局，不要大量依赖无意义的固定宽高和绝对定位；但如果参考实现中某些局部元素本来就适合固定尺寸或定位，可以保留。
11. 文本内容不要写死 height；长文本、长数字、地址等内容要允许换行或断行。
12. 当前输入代码可能包含大量机器生成的无语义 class 名、冗余样式、重复结构和不合理的固定宽高。允许你在不改变文案含义和业务逻辑的前提下，重构 class 名、合并样式、精简 DOM、优化结构。
13. 命名必须可维护，不要输出 `shared-style-1`、`node-xx`、`mastergo-node-xx` 这类机器命名。
14. 如果导出代码已经在某些局部正确表达了参考样式，可以保留；如果存在明显不合理的布局、重复结构、样式噪音或脱离项目风格的问题，应主动优化。
15. 最终目标不是“尽量像导出稿”，而是“尽量像这个项目里真正会被合并和维护的页面代码”。

输出要求：
1. 输出最终可运行的完整 Vue 单文件组件代码。
2. 默认按真实项目代码风格输出，不要输出演示页、展示壳或纯还原稿。
3. 如果适合复用现有公共组件，优先按组件组合方式输出，而不是整页纯手写。
4. 最后附一段简短说明，说明：
   - 做了哪些响应式处理
   - 复用了哪些现有公共组件或参考了哪些现有实现
   - 哪些地方对导出代码做了结构性重构
   - 哪些地方保留了固定尺寸或绝对定位，以及原因
