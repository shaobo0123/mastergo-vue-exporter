你现在要对我提供的导出 Vue 代码做二次整理和响应式改造，输出可维护的最终 Vue 页面代码。

要求如下：
1. 这是手机页面，顶部手机状态栏不要绘制。
2. 页面要尽量一比一还原原始设计效果，不要随意改动层级、间距、对齐、字号、颜色、圆角、阴影和整体结构。
3. width 不要固定，不能把整页写死成 375px，页面必须是响应式布局。
4. 优先使用 flex 或 grid，不要大量依赖固定宽高和绝对定位。
5. 页面根容器优先使用 `width: 100%`、`min-height: 100vh`；如需限制宽度，可使用 `max-width` + `margin: 0 auto`。
6. 文本内容不要写死 height；图片尽量使用 `max-width: 100%`、高度自适应。
7. 图标、头像、按钮高度、分割线、状态角标、少量装饰元素可以保留固定尺寸。
8. 当前输入代码可能包含大量机器生成的无语义 class 名、冗余样式、重复结构和不合理的固定宽高。允许你在不改变文案含义和业务交互逻辑的前提下，重构 class 名、合并样式、精简 DOM、优化结构。
9. 不要输出 `shared-style-1`、`node-xx`、`mastergo-node-xx` 这类无语义命名，命名要可维护。
10. 优先复用项目里已有的公共组件和页面骨架，不要重复造轮子。包括但不限于：
   - `mu-header` / `mu-footer`
   - `van-nav-bar` / `common-footer`
   - `MainContainer`
   - `MuNoData`
   - `MisSceneHeader`
   - 以及项目中已有的卡片、列表、表单、骨架屏组件
11. 如果原始导出代码已经正确表达设计效果，可以保留；如果存在明显不合理的固定宽高、重复样式、无语义命名、过多绝对定位，应主动优化。
12. 你的任务不是重新自由发挥设计，而是把导出的代码整理成真实项目中可落地、可维护的代码。

输出要求：
1. 输出最终可运行的完整 Vue 单文件组件代码。
2. 最后附一段简短说明，说明：
   - 做了哪些响应式处理
   - 复用了哪些现有公共组件
   - 哪些地方保留了固定尺寸或绝对定位
